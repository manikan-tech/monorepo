# Private runtime configuration for the Body container.
# Copy to infra/env/body.env; never commit the copy.
# This value must exactly match BODY_SERVICE_KEY in store.env.
BODY_SERVICE_KEY=<long-random-shared-secret>

# Optional only while rotating the shared secret:
# BODY_SERVICE_KEY_PREVIOUS=<previous-secret>
