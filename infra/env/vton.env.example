# Private runtime configuration for the VTON container.
# Copy to infra/env/vton.env; never commit the copy.
# The internal key must exactly match Store's TRYON_SERVICE_KEY.
FASHN_API_KEY=<FASHN.ai Developer API key>
TRYON_SERVICE_KEY=<same-value-as-store.env>
