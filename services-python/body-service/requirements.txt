# Install CPU-only torch from the PyTorch index; everything else from PyPI.
# This keeps the Docker image / Railway deploy small (no CUDA libraries).
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
torch>=2.2.0
smplx>=0.1.28
trimesh>=4.0.0
numpy>=1.26.0,<2.0
scipy>=1.12.0
