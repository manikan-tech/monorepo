# Unlisted proof-of-concept

This file deliberately has **no entry in `nav.ts`**. It exists only to verify the review-before-publish rule: it should not appear in the sidebar, should not appear in search, and visiting its URL directly (`/docs/garments/_unlisted-proof`) should return a normal 404, not this content.

If you can see this rendered on the actual docs site anywhere, the publish gate is broken.
